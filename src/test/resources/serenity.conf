webdriver {
  driver = chrome
  autodownload = true
}

serenity.project.name = "SauceDemo E2E"
serenity.take.screenshots = AFTER_EACH_STEP
serenity.logging = VERBOSE

chrome.switches = [
  "--remote-allow-origins=*",
  "--start-maximized"
]
